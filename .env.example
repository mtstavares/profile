# Public settings only. Leave URL empty for a local, noindex build.
# Production: set the actual HTTPS GitHub Pages URL, including repository path.
VITE_SITE_URL=
# Optional: defaults to the URL pathname, or / when URL is empty.
VITE_BASE_PATH=
